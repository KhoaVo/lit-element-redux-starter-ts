import { css } from 'lit-element';

export const sharedStyles = css`
  a {
    color: inherit;
  }

  button {
    padding: 0;
    background: none;
    border: 0;
    color: inherit;
    cursor: pointer;
  }
`;
